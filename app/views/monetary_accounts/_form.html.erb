<%= form_with(model: monetary_account, local: true) do |form| %>
  <% if monetary_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(monetary_account.errors.count, "error") %> prohibited this monetary_account from being saved:</h2>

      <ul>
      <% monetary_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :account_id %>
    <%= form.number_field :account_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
